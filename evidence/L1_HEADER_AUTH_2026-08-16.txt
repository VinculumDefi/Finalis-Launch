

  W0 · harness smoke
    √ deploys every contract in the system (2351ms)
    √ tokens report their configured hard caps (280ms)

  CL-04 · stake duration multipliers must match §10.1
    √ duration 30d multiplier is 1x (255ms)
    √ duration 60d multiplier is 1.4x (212ms)
    √ duration 90d multiplier is 1.75x (229ms)
    √ duration 120d multiplier is 2x (222ms)

  CL-02 · VF-IMM-001/VF-DEP-006: authority terminated at finalization
    √ initialize() cannot be called twice (277ms)
    √ deployer address is zeroed - authority irreversibly terminated (256ms)
    √ finalized flag is independently verifiable on-chain (VF-DEP-007) (261ms)
    √ no address can seize minting after finalization (239ms)
    √ the original deployer has no residual minting power (234ms)
    √ setMinter no longer exists (214ms)

  CL-07 · stake contract can mint epoch rewards
    √ stake contract is an authorized VCLM minter (197ms)
    √ verifier is also an authorized VCLM minter (218ms)
    √ VF-STK-004: stake has no CHONX minting privilege (206ms)

  CL-03 · §10.1 token multipliers (VCLM 1.0x / CHONX 2.0x / SYNTH 4.0x)
    √ VCLM weight at 30d is amount x 1.0 x 1.0 (229ms)
    √ CHONX weight is exactly twice VCLM at equal amount and duration (470ms)
    √ SYNTH weight is exactly four times VCLM at equal amount and duration (446ms)

  CL-14 · VF-STK-025 expired position cannot backdate over a gap
    √ queueExtension reverts once the position has expired (241ms)

  CL-02 · Verifier: deployment ceremony closes irreversibly
    √ registerChainVerifier is unreachable after finalization (196ms)
    √ registerAssetPrecision is unreachable after finalization (202ms)
    √ configureDevFund is unreachable after finalization (204ms)
    √ finalize() cannot be called twice (192ms)
    √ verifier deployer is zeroed - authority terminated (VF-DEP-006) (186ms)
    √ finalized flag is publicly readable (VF-DEP-007) (190ms)
    √ no attacker can install a chain verifier that validates forged locks (201ms)
    √ VF-DEP-002: a zero chain verifier cannot be registered (103ms)

  CL-02 · Synth and Stake carry no authority
    √ Synth setVerifier no longer exists (199ms)
    √ Synth setTokenContracts no longer exists (191ms)
    √ Synth burn targets are immutable (189ms)
    √ Stake exposes no authority address (CL-29) (198ms)

  VF-DEP-001 · issuance inactive until finalization
    √ verifyAndMint reverts before the ceremony is closed (114ms)

  CL-01 · VF-ORC-007 signed price records
    √ the arbitrary-issuance parameter no longer exists (231ms)
    √ recordFeeAndRac no longer accepts a caller USD value (198ms)
    √ accepts a batch signed by the price publisher (209ms)
    √ rejects a batch signed by anyone else (246ms)
    √ rejects a tampered price with a valid signature over different data (277ms)
    √ VF-SEC-005: any address may submit; the submitter gains no authority (300ms)
    √ VF-ORC-008: a run must be newer than the last accepted run (193ms)
    √ VF-ORC-005: a zero price marks the asset unavailable rather than substituting one (218ms)
    √ a signature cannot be replayed against a different deployment (255ms)

  CL-01 · price publisher is immutable
    √ VF-DEP-002: a zero price publisher cannot be constructed (227ms)
    √ no function can change the price publisher (195ms)

  CL-39 · run watermark cannot be pushed beyond recovery
    √ rejects a run near the uint64 maximum (183ms)
    √ rejects an advance one beyond the permitted window (199ms)
    √ accepts an advance at the permitted boundary (199ms)
    √ the window advances with the watermark, so long gaps stay recoverable (199ms)
    √ normal consecutive runs are unaffected (206ms)

  CL-37 · stale price records fail closed
    √ a fresh record has a usable valuation (197ms)
    √ remains usable just inside 48 hours (219ms)
    √ is no longer usable past 48 hours (207ms)
    √ an unavailable asset is never usable regardless of age (207ms)
    √ an asset never priced has no usable valuation (187ms)
    √ a new batch restores usability after staleness (235ms)
    √ age is measured from the signed fetch timestamp, not from submission (224ms)

  CL-11 · VF-COM-006 handshake allowance registry
    √ registers a one-use mechanism (113ms)
    √ registers a three-use mechanism (103ms)
    √ VF-COM-006: rejects any allowance other than 1 or 3 (132ms)
    √ an unregistered environment reports zero (100ms)
    √ allowance cannot be registered after finalization (174ms)
    √ allowance cannot be changed by a stranger during the ceremony (109ms)
    √ the package field is no longer consulted anywhere in enforcement

  End-to-end · full verification pipeline via MockChainVerifier
    √ a valid handshake mints VCLM (225ms)
    √ CL-01: issuance is impossible without a published price (210ms)
    √ CL-37: issuance fails closed once the price is stale (212ms)
    √ VF-XCH-011: a package contradicting the source facts is rejected (219ms)
    √ VF-XCH-006: an unfinalized source event is rejected (270ms)

  CL-11 · handshake allowance consumption, end to end
    √ VF-COM-007: a three-use mechanism permits exactly three, rejects the fourth (328ms)
    √ VF-COM-007: a one-use mechanism permits exactly one, rejects the second (249ms)
    √ allowance is per identity, not global (233ms)
    √ CL-11: the caller's asserted allowance count is ignored entirely (252ms)
    √ VF-COM-008: a rejected attempt consumes no allowance (312ms)

  CL-12 · Dev Fund destination enforcement
    √ VF-FEE-006: a substituted destination is rejected (223ms)
    √ the registered destination is accepted (196ms)
    √ an empty destination is rejected even if the registry were empty (218ms)
    √ VF-FEE-008: missing fee transfer evidence is rejected (233ms)
    √ VF-FEE-009: an unconfigured environment cannot issue (155ms)
    √ VF-FEE-009: an empty destination cannot be registered at all (106ms)

  CL-06 · epoch reward basis is populated from RAC credits
    √ the verifier accumulates a running per-epoch reward basis (191ms)
    √ credits accumulate additively within one epoch (235ms)
    √ closeEpoch reads the basis instead of leaving it zero (247ms)
    √ an epoch with no credits has a zero basis and pays nothing (234ms)

  CL-07 · the Stake contract actually mints epoch rewards
    √ a staker is paid through closeEpoch, allocateEpoch and claim (267ms)
    √ VF-STK-004: the reward is newly minted VCLM, raising total supply (271ms)
    √ VF-STK-015: an epoch with no stakers allocates nothing (220ms)

  CL-09 · epoch processing cost must not grow with position count
        closeEpoch gas: 5 positions=109284, 60 positions=109284, ratio=1.00x
    √ closeEpoch gas is bounded as positions accumulate (633ms)
        closeEpoch gas: 5=109284, 600=109284, ratio=1.000x
    √ closeEpoch gas is flat from 5 to 600 lifetime positions (3850ms)
        allocateEpoch gas: 5=53271, 60=53271, ratio=1.00x
    √ allocateEpoch gas is bounded as positions accumulate (667ms)

  CL-09 · differential oracle — accumulator vs specification
    √ agrees with brute force across 20 randomized positions and 6 epochs (547ms)
    √ agrees after a withdrawal cancels future contribution (213ms)
    √ conservation: registered weight matches the accumulator at every boundary (293ms)

  CL-41 PROOF OF EXPLOIT · unvalidated assetPrecision in recordFeeAndRac
        honest credit: 600000000000000000
        attack credit: 600000000000000000
        inflation:     1x
    √ a caller can inflate the epoch reward basis by understating decimals (166ms)

  CL-41 · pkg.assetPrecision is economically inert
        credits by declared precision [0,6,12,18,255]: 600000000000000000, 600000000000000000, 600000000000000000, 600000000000000000, 600000000000000000
    √ credit is identical across 0, 6, 12, 18 and 255 declared precision (178ms)

  CL-41 · precision divisor comes from the registry
    √ no USD derivation divides by pkg.assetPrecision

  CL-42 · custody class must not silently default
    √ VF-SEC-003: an unrecognized custody class is rejected at registration (132ms)
    √ the three valid classes are accepted (110ms)
    √ custody path is bounded to native or token (97ms)

  CL-43 · asset precision must be within the executable domain
    √ a precision beyond safe arithmetic is rejected at registration (124ms)
    √ precisions across the real range are accepted (139ms)

  CL-44 · output token discriminator is closed
    √ VF-COM-020: every value outside {0,1} is rejected before any minting (242ms)
    √ VCLM (0) mints VCLM and not CHONX (166ms)
    √ rejection precedes emission-rate selection, so no invalid rate is ever used

  CL-45 · staking token discriminator is closed
    √ VF-STK-002: every value outside {0,1,2} is rejected (170ms)
    √ all three valid tokens can be staked (185ms)

  CL-46 · maximum multiplier product does not overflow
    √ SYNTH at 120 days — the largest product — computes exactly 8x (160ms)
    √ a very large stake at maximum multipliers does not revert (150ms)
    √ the weight expression keeps the uint256 operand leftmost

  CL-47 · duration discriminator admits only the specified terms
    √ §10.1: an unlisted duration is rejected (194ms)

  CL-76 · REGRESSION — forged packages are rejected by the production UtxoChainVerifier

    CL-76 regression: minted 0.0 VCLM from a forged package
    Caller: 0x9965507D1a55bcC2695C58ba16FB37d819B0A4dc (no deployer role, no lock)

    √ refuses to mint for an unprivileged caller with no lock on any chain (270ms)
    √ the production verifier refuses an invented block (243ms)

    CL-76 regression, repeat: minted 0.0 VCLM from a second phantom lock

    √ a second forged handshake is refused just as the first was (254ms)

  Base Commitment Vault — creation
    √ creates a lock, splits the fee, and isolates principal in its own contract (223ms)
    √ accepts all 16 permitted durations and rejects durations adjacent to them (374ms)

  Base Commitment Vault — negative cases (Standard §5.2)
    √ rejects an unapproved asset WITHOUT moving any of the user's funds (234ms)
    √ rejects a fee-on-transfer asset rather than locking a different amount than recorded (198ms)
    √ rejects a duplicate lock id (184ms)
    √ rejects a zero base recipient and a zero release destination (201ms)
    √ rejects a standard lock below $10.00 (190ms)
    √ rejects a Handshake outside the $0.95-$1.05 band (254ms)
    √ rejects a fourth Handshake from the same identity (223ms)
    √ rejects a CHONX output with no activation receipt (179ms)
    √ rejects ETH sent alongside an ERC-20 lock (187ms)

  Base Commitment Vault — valuation is derived, not asserted
    √ the same gross amount qualifies or fails purely on the oracle price (386ms)
    √ records the derived USD value in the immutable lock record (199ms)

  Base Commitment Vault — release
    √ refuses to release before maturity (225ms)
    √ releases at maturity to the bound destination when called by a stranger (231ms)
    √ refuses a second release (237ms)
    √ releases without consulting the verifier, the price feed, or the factory (233ms)

  Base Commitment Vault — isolation by construction
    √ releasing one lock leaves every other lock untouched (290ms)
    √ the cloned implementation cannot be re-initialized (215ms)
    √ the implementation contract itself is permanently disabled (227ms)

  Base Commitment Vault — configuration
    √ refuses lock creation before configuration is finalized (124ms)
    √ refuses asset registration after finalization, and from a non-deployer (208ms)

  CL-48 · fetchTimestamp is bounded in both temporal directions
    √ BACKWARD: a record older than 48 hours is not usable (CL-37) (115ms)
    √ FORWARD: a future fetchTimestamp is rejected at submission (132ms)
    √ a future timestamp cannot extend the freshness window, because it cannot be stored (135ms)
    √ a fetchTimestamp equal to the current block is accepted (116ms)

  CL-49 · runId cannot wrap, skip beyond recovery, or be reused
    √ the maximum permitted advance succeeds (99ms)
    √ one beyond the permitted advance is rejected (101ms)
    √ a run id cannot be reused, so no batch can be replayed (133ms)
    √ no earlier run id can be accepted after a later one (154ms)

  BaseSameChainVerifier — rejects forged evidence
    √ reverts on a lock that does not exist, however well-formed the proof (234ms)
    √ ignores the caller's claimed amounts and returns vault storage instead (268ms)
    √ ignores the finality proof entirely (355ms)
    √ derives the block hash and height rather than echoing the caller's (329ms)

  BaseSameChainVerifier — accepts genuine locks
    √ verifies a real lock and reports it finalized (344ms)
    √ reports an unknown lock as unverifiable without reverting (1223ms)

  BaseSameChainVerifier — construction
    √ refuses a zero vault address (345ms)
    √ binds the vault immutably at construction (995ms)

  Base end-to-end — genuine lock through the production verifier

    Base e2e: minted 1725.0 VCLM against a real lock

    √ mints against a real Base lock, called by an unprivileged relayer (732ms)
    √ element 9 — the same lock cannot mint twice (1335ms)
    √ element 10 — a failed attempt consumes nothing and the lock stays mintable (572ms)
    √ a package naming a lock that was never created cannot mint (367ms)

    Base verifyAndMint gas: 213527

    √ element 8 — records the gas cost of same-chain verification (338ms)

  Sha256dHeaderChain — proof of work
    √ accepts a real header whose work meets its declared target (60ms)
    √ accepts consecutive headers in one call (79ms)
    √ rejects a header whose nonce has been altered (57ms)
    √ rejects a header with an altered merkle root (51ms)
    √ rejects a header whose parent is unknown (48ms)
    √ rejects a malformed header length (52ms)
    √ rejects a difficulty change off a retarget boundary

  Sha256dHeaderChain — chain state
    √ is idempotent: resubmitting a known header is a no-op (54ms)
    √ reports confirmation depth from the best tip (47ms)
    √ reports zero confirmations for an unknown block
    √ accumulates work rather than counting height (42ms)
    √ refuses a zero checkpoint

  Sha256dHeaderChain — merkle inclusion
    √ verifies the coinbase of a single-transaction block (52ms)
    √ rejects a transaction that is not in the block (46ms)
    √ reverts for an unknown block

  UtxoChainVerifier — C.8 fact extraction
    √ derives every lock fact from the transaction bytes (86ms)
    √ rejects a witness script that does not match the output commitment (78ms)
    √ rejects a principal output that is not P2WSH (87ms)
    √ rejects a maturity expressed as a block height (88ms)
    √ rejects a script with a trailing opcode after OP_CHECKSIG (84ms)
    √ rejects identical principal and fee output indices (71ms)
    √ exposes the canonical release public key as the handshake identity (85ms)

  UtxoChainVerifier — inclusion and depth
    √ verifies finality for an included transaction at sufficient depth (92ms)
    √ rejects a transaction not committed to by the block (72ms)
    √ rejects a block the header chain has never seen (82ms)
    √ rejects insufficient confirmation depth (73ms)
    √ extractFacts re-verifies inclusion rather than trusting a prior call (74ms)
    √ isFinal reports without reverting (86ms)

  UtxoChainVerifier — construction
    √ refuses a zero header chain address
    √ binds configuration immutably and exposes no setter (73ms)

  MerklePatriciaProof — single-leaf trie
    √ recovers the value from a leaf node whose hash is the root (49ms)
    √ rejects a proof whose node does not hash to the root (38ms)
    √ rejects a leaf whose path does not match the key (46ms)
    √ rejects an empty proof

  MerklePatriciaProof — branch node
    √ descends a branch to the correct leaf (77ms)
    √ rejects a key routed to an empty branch slot (49ms)
    √ rejects a leaf swapped between branch slots (45ms)

  MerklePatriciaProof — receipt decoding
    √ strips the type byte from a typed receipt and leaves legacy untouched (60ms)
    √ decodes a receipt's status field
    √ reports a failed receipt as status zero

  L1BlockRegistry — recording
    √ records the block number and hash reported by the predeploy (86ms)
    √ is permissionless — any caller may record, none may influence content (79ms)
    √ accumulates history as the L1 origin advances (146ms)
    √ is idempotent when the L1 origin has not advanced (65ms)
    √ refuses to record an empty origin (57ms)
    √ refuses a zero predeploy address at construction
    √ exposes the canonical OP Stack predeploy address (57ms)

  L1BlockRegistry — header verification
    √ extracts receiptsRoot from a header matching the recorded hash (73ms)
    √ rejects a header that does not hash to the recorded commitment (63ms)
    √ rejects a block number that was never recorded (51ms)
    √ rejects a truncated header (62ms)
    √ isRecorded reports without reverting (80ms)


  206 passing (47s)

