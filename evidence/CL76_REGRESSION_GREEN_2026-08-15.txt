

  W0 · harness smoke
    √ deploys every contract in the system (2490ms)
    √ tokens report their configured hard caps (227ms)

  CL-04 · stake duration multipliers must match §10.1
    √ duration 30d multiplier is 1x (244ms)
    √ duration 60d multiplier is 1.4x (240ms)
    √ duration 90d multiplier is 1.75x (237ms)
    √ duration 120d multiplier is 2x (259ms)

  CL-02 · VF-IMM-001/VF-DEP-006: authority terminated at finalization
    √ initialize() cannot be called twice (269ms)
    √ deployer address is zeroed - authority irreversibly terminated (300ms)
    √ finalized flag is independently verifiable on-chain (VF-DEP-007) (285ms)
    √ no address can seize minting after finalization (291ms)
    √ the original deployer has no residual minting power (220ms)
    √ setMinter no longer exists (235ms)

  CL-07 · stake contract can mint epoch rewards
    √ stake contract is an authorized VCLM minter (214ms)
    √ verifier is also an authorized VCLM minter (232ms)
    √ VF-STK-004: stake has no CHONX minting privilege (612ms)

  CL-03 · §10.1 token multipliers (VCLM 1.0x / CHONX 2.0x / SYNTH 4.0x)
    √ VCLM weight at 30d is amount x 1.0 x 1.0 (282ms)
    √ CHONX weight is exactly twice VCLM at equal amount and duration (445ms)
    √ SYNTH weight is exactly four times VCLM at equal amount and duration (454ms)

  CL-14 · VF-STK-025 expired position cannot backdate over a gap
    √ queueExtension reverts once the position has expired (202ms)

  CL-02 · Verifier: deployment ceremony closes irreversibly
    √ registerChainVerifier is unreachable after finalization (222ms)
    √ registerAssetPrecision is unreachable after finalization (206ms)
    √ configureDevFund is unreachable after finalization (195ms)
    √ finalize() cannot be called twice (188ms)
    √ verifier deployer is zeroed - authority terminated (VF-DEP-006) (187ms)
    √ finalized flag is publicly readable (VF-DEP-007) (198ms)
    √ no attacker can install a chain verifier that validates forged locks (206ms)
    √ VF-DEP-002: a zero chain verifier cannot be registered (119ms)

  CL-02 · Synth and Stake carry no authority
    √ Synth setVerifier no longer exists (199ms)
    √ Synth setTokenContracts no longer exists (192ms)
    √ Synth burn targets are immutable (207ms)
    √ Stake exposes no authority address (CL-29) (211ms)

  VF-DEP-001 · issuance inactive until finalization
    √ verifyAndMint reverts before the ceremony is closed (118ms)

  CL-01 · VF-ORC-007 signed price records
    √ the arbitrary-issuance parameter no longer exists (212ms)
    √ recordFeeAndRac no longer accepts a caller USD value (197ms)
    √ accepts a batch signed by the price publisher (206ms)
    √ rejects a batch signed by anyone else (229ms)
    √ rejects a tampered price with a valid signature over different data (210ms)
    √ VF-SEC-005: any address may submit; the submitter gains no authority (248ms)
    √ VF-ORC-008: a run must be newer than the last accepted run (201ms)
    √ VF-ORC-005: a zero price marks the asset unavailable rather than substituting one (230ms)
    √ a signature cannot be replayed against a different deployment (249ms)

  CL-01 · price publisher is immutable
    √ VF-DEP-002: a zero price publisher cannot be constructed (243ms)
    √ no function can change the price publisher (184ms)

  CL-39 · run watermark cannot be pushed beyond recovery
    √ rejects a run near the uint64 maximum (197ms)
    √ rejects an advance one beyond the permitted window (193ms)
    √ accepts an advance at the permitted boundary (208ms)
    √ the window advances with the watermark, so long gaps stay recoverable (208ms)
    √ normal consecutive runs are unaffected (212ms)

  CL-37 · stale price records fail closed
    √ a fresh record has a usable valuation (204ms)
    √ remains usable just inside 48 hours (225ms)
    √ is no longer usable past 48 hours (222ms)
    √ an unavailable asset is never usable regardless of age (211ms)
    √ an asset never priced has no usable valuation (232ms)
    √ a new batch restores usability after staleness (239ms)
    √ age is measured from the signed fetch timestamp, not from submission (234ms)

  CL-11 · VF-COM-006 handshake allowance registry
    √ registers a one-use mechanism (111ms)
    √ registers a three-use mechanism (105ms)
    √ VF-COM-006: rejects any allowance other than 1 or 3 (135ms)
    √ an unregistered environment reports zero (93ms)
    √ allowance cannot be registered after finalization (180ms)
    √ allowance cannot be changed by a stranger during the ceremony (133ms)
    √ the package field is no longer consulted anywhere in enforcement

  End-to-end · full verification pipeline via MockChainVerifier
    √ a valid handshake mints VCLM (226ms)
    √ CL-01: issuance is impossible without a published price (217ms)
    √ CL-37: issuance fails closed once the price is stale (196ms)
    √ VF-XCH-011: a package contradicting the source facts is rejected (252ms)
    √ VF-XCH-006: an unfinalized source event is rejected (299ms)

  CL-11 · handshake allowance consumption, end to end
    √ VF-COM-007: a three-use mechanism permits exactly three, rejects the fourth (361ms)
    √ VF-COM-007: a one-use mechanism permits exactly one, rejects the second (220ms)
    √ allowance is per identity, not global (273ms)
    √ CL-11: the caller's asserted allowance count is ignored entirely (247ms)
    √ VF-COM-008: a rejected attempt consumes no allowance (320ms)

  CL-12 · Dev Fund destination enforcement
    √ VF-FEE-006: a substituted destination is rejected (200ms)
    √ the registered destination is accepted (235ms)
    √ an empty destination is rejected even if the registry were empty (224ms)
    √ VF-FEE-008: missing fee transfer evidence is rejected (225ms)
    √ VF-FEE-009: an unconfigured environment cannot issue (153ms)
    √ VF-FEE-009: an empty destination cannot be registered at all (86ms)

  CL-06 · epoch reward basis is populated from RAC credits
    √ the verifier accumulates a running per-epoch reward basis (231ms)
    √ credits accumulate additively within one epoch (248ms)
    √ closeEpoch reads the basis instead of leaving it zero (266ms)
    √ an epoch with no credits has a zero basis and pays nothing (220ms)

  CL-07 · the Stake contract actually mints epoch rewards
    √ a staker is paid through closeEpoch, allocateEpoch and claim (296ms)
    √ VF-STK-004: the reward is newly minted VCLM, raising total supply (285ms)
    √ VF-STK-015: an epoch with no stakers allocates nothing (243ms)

  CL-09 · epoch processing cost must not grow with position count
        closeEpoch gas: 5 positions=109284, 60 positions=109284, ratio=1.00x
    √ closeEpoch gas is bounded as positions accumulate (680ms)
        closeEpoch gas: 5=109284, 600=109284, ratio=1.000x
    √ closeEpoch gas is flat from 5 to 600 lifetime positions (4203ms)
        allocateEpoch gas: 5=53271, 60=53271, ratio=1.00x
    √ allocateEpoch gas is bounded as positions accumulate (684ms)

  CL-09 · differential oracle — accumulator vs specification
    √ agrees with brute force across 20 randomized positions and 6 epochs (607ms)
    √ agrees after a withdrawal cancels future contribution (218ms)
    √ conservation: registered weight matches the accumulator at every boundary (321ms)

  CL-41 PROOF OF EXPLOIT · unvalidated assetPrecision in recordFeeAndRac
        honest credit: 600000000000000000
        attack credit: 600000000000000000
        inflation:     1x
    √ a caller can inflate the epoch reward basis by understating decimals (185ms)

  CL-41 · pkg.assetPrecision is economically inert
        credits by declared precision [0,6,12,18,255]: 600000000000000000, 600000000000000000, 600000000000000000, 600000000000000000, 600000000000000000
    √ credit is identical across 0, 6, 12, 18 and 255 declared precision (199ms)

  CL-41 · precision divisor comes from the registry
    √ no USD derivation divides by pkg.assetPrecision

  CL-42 · custody class must not silently default
    √ VF-SEC-003: an unrecognized custody class is rejected at registration (117ms)
    √ the three valid classes are accepted (125ms)
    √ custody path is bounded to native or token (100ms)

  CL-43 · asset precision must be within the executable domain
    √ a precision beyond safe arithmetic is rejected at registration (130ms)
    √ precisions across the real range are accepted (147ms)

  CL-44 · output token discriminator is closed
    √ VF-COM-020: every value outside {0,1} is rejected before any minting (264ms)
    √ VCLM (0) mints VCLM and not CHONX (159ms)
    √ rejection precedes emission-rate selection, so no invalid rate is ever used

  CL-45 · staking token discriminator is closed
    √ VF-STK-002: every value outside {0,1,2} is rejected (178ms)
    √ all three valid tokens can be staked (195ms)

  CL-46 · maximum multiplier product does not overflow
    √ SYNTH at 120 days — the largest product — computes exactly 8x (173ms)
    √ a very large stake at maximum multipliers does not revert (159ms)
    √ the weight expression keeps the uint256 operand leftmost

  CL-47 · duration discriminator admits only the specified terms
    √ §10.1: an unlisted duration is rejected (191ms)

  CL-76 · REGRESSION — forged packages are rejected by the production UtxoChainVerifier

    CL-76 regression: minted 0.0 VCLM from a forged package
    Caller: 0x9965507D1a55bcC2695C58ba16FB37d819B0A4dc (no deployer role, no lock)

    √ refuses to mint for an unprivileged caller with no lock on any chain (260ms)
    √ the production verifier refuses an invented block (246ms)

    CL-76 regression, repeat: minted 0.0 VCLM from a second phantom lock

    √ a second forged handshake is refused just as the first was (266ms)

  Base Commitment Vault — creation
    √ creates a lock, splits the fee, and isolates principal in its own contract (226ms)
    √ accepts all 16 permitted durations and rejects durations adjacent to them (463ms)

  Base Commitment Vault — negative cases (Standard §5.2)
    √ rejects an unapproved asset WITHOUT moving any of the user's funds (276ms)
    √ rejects a fee-on-transfer asset rather than locking a different amount than recorded (265ms)
    √ rejects a duplicate lock id (227ms)
    √ rejects a zero base recipient and a zero release destination (220ms)
    √ rejects a standard lock below $10.00 (207ms)
    √ rejects a Handshake outside the $0.95-$1.05 band (221ms)
    √ rejects a fourth Handshake from the same identity (220ms)
    √ rejects a CHONX output with no activation receipt (189ms)
    √ rejects ETH sent alongside an ERC-20 lock (205ms)

  Base Commitment Vault — valuation is derived, not asserted
    √ the same gross amount qualifies or fails purely on the oracle price (403ms)
    √ records the derived USD value in the immutable lock record (180ms)

  Base Commitment Vault — release
    √ refuses to release before maturity (229ms)
    √ releases at maturity to the bound destination when called by a stranger (250ms)
    √ refuses a second release (254ms)
    √ releases without consulting the verifier, the price feed, or the factory (222ms)

  Base Commitment Vault — isolation by construction
    √ releasing one lock leaves every other lock untouched (319ms)
    √ the cloned implementation cannot be re-initialized (228ms)
    √ the implementation contract itself is permanently disabled (238ms)

  Base Commitment Vault — configuration
    √ refuses lock creation before configuration is finalized (130ms)
    √ refuses asset registration after finalization, and from a non-deployer (192ms)

  CL-48 · fetchTimestamp is bounded in both temporal directions
    √ BACKWARD: a record older than 48 hours is not usable (CL-37) (121ms)
    √ FORWARD: a future fetchTimestamp is rejected at submission (160ms)
    √ a future timestamp cannot extend the freshness window, because it cannot be stored (131ms)
    √ a fetchTimestamp equal to the current block is accepted (139ms)

  CL-49 · runId cannot wrap, skip beyond recovery, or be reused
    √ the maximum permitted advance succeeds (101ms)
    √ one beyond the permitted advance is rejected (101ms)
    √ a run id cannot be reused, so no batch can be replayed (163ms)
    √ no earlier run id can be accepted after a later one (152ms)

  BaseSameChainVerifier — rejects forged evidence
    √ reverts on a lock that does not exist, however well-formed the proof (241ms)
    √ ignores the caller's claimed amounts and returns vault storage instead (265ms)
    √ ignores the finality proof entirely (246ms)
    √ derives the block hash and height rather than echoing the caller's (243ms)

  BaseSameChainVerifier — accepts genuine locks
    √ verifies a real lock and reports it finalized (257ms)
    √ reports an unknown lock as unverifiable without reverting (231ms)

  BaseSameChainVerifier — construction
    √ refuses a zero vault address
    √ binds the vault immutably at construction (228ms)

  Base end-to-end — genuine lock through the production verifier

    Base e2e: minted 1725.0 VCLM against a real lock

    √ mints against a real Base lock, called by an unprivileged relayer (315ms)
    √ element 9 — the same lock cannot mint twice (355ms)
    √ element 10 — a failed attempt consumes nothing and the lock stays mintable (344ms)
    √ a package naming a lock that was never created cannot mint (295ms)

    Base verifyAndMint gas: 213527

    √ element 8 — records the gas cost of same-chain verification (338ms)

  Sha256dHeaderChain — proof of work
    √ accepts a real header whose work meets its declared target (69ms)
    √ accepts consecutive headers in one call (72ms)
    √ rejects a header whose nonce has been altered (44ms)
    √ rejects a header with an altered merkle root (40ms)
    √ rejects a header whose parent is unknown (49ms)
    √ rejects a malformed header length (39ms)
    √ rejects a difficulty change off a retarget boundary

  Sha256dHeaderChain — chain state
    √ is idempotent: resubmitting a known header is a no-op
    √ reports confirmation depth from the best tip (41ms)
    √ reports zero confirmations for an unknown block
    √ accumulates work rather than counting height
    √ refuses a zero checkpoint

  Sha256dHeaderChain — merkle inclusion
    √ verifies the coinbase of a single-transaction block (57ms)
    √ rejects a transaction that is not in the block (70ms)
    √ reverts for an unknown block (40ms)

  UtxoChainVerifier — SPV-backed finality
    √ verifies a transaction included in a proof-of-work-validated block (108ms)
    √ rejects a block the header chain has never seen (81ms)
    √ rejects a transaction that is not in the block (75ms)
    √ rejects a valid transaction paired with the wrong block (74ms)
    √ rejects insufficient confirmation depth (67ms)
    √ reads depth from the header chain, not from the caller (85ms)
    √ isFinal reports without reverting (94ms)

  UtxoChainVerifier — extractFacts is blocked by CL-27
    √ fails closed rather than returning caller-decoded values (63ms)

  UtxoChainVerifier — construction
    √ refuses a zero header chain address
    √ binds the header chain immutably and exposes no setter (77ms)


  179 passing (43s)

