

  W0 · harness smoke
    √ deploys every contract in the system (2600ms)
    √ tokens report their configured hard caps (247ms)

  CL-04 · stake duration multipliers must match §10.1
    √ duration 30d multiplier is 1x (237ms)
    √ duration 60d multiplier is 1.4x (233ms)
    √ duration 90d multiplier is 1.75x (244ms)
    √ duration 120d multiplier is 2x (239ms)

  CL-02 · VF-IMM-001/VF-DEP-006: authority terminated at finalization
    √ initialize() cannot be called twice (257ms)
    √ deployer address is zeroed - authority irreversibly terminated (334ms)
    √ finalized flag is independently verifiable on-chain (VF-DEP-007) (276ms)
    √ no address can seize minting after finalization (284ms)
    √ the original deployer has no residual minting power (191ms)
    √ setMinter no longer exists (241ms)

  CL-07 · stake contract can mint epoch rewards
    √ stake contract is an authorized VCLM minter (229ms)
    √ verifier is also an authorized VCLM minter (232ms)
    √ VF-STK-004: stake has no CHONX minting privilege (181ms)

  CL-03 · §10.1 token multipliers (VCLM 1.0x / CHONX 2.0x / SYNTH 4.0x)
    √ VCLM weight at 30d is amount x 1.0 x 1.0 (237ms)
    √ CHONX weight is exactly twice VCLM at equal amount and duration (481ms)
    √ SYNTH weight is exactly four times VCLM at equal amount and duration (405ms)

  CL-14 · VF-STK-025 expired position cannot backdate over a gap
    √ queueExtension reverts once the position has expired (244ms)

  CL-02 · Verifier: deployment ceremony closes irreversibly
    √ registerChainVerifier is unreachable after finalization (207ms)
    √ registerAssetPrecision is unreachable after finalization (202ms)
    √ configureDevFund is unreachable after finalization (173ms)
    √ finalize() cannot be called twice (182ms)
    √ verifier deployer is zeroed - authority terminated (VF-DEP-006) (205ms)
    √ finalized flag is publicly readable (VF-DEP-007) (209ms)
    √ no attacker can install a chain verifier that validates forged locks (210ms)
    √ VF-DEP-002: a zero chain verifier cannot be registered (98ms)

  CL-02 · Synth and Stake carry no authority
    √ Synth setVerifier no longer exists (183ms)
    √ Synth setTokenContracts no longer exists (208ms)
    √ Synth burn targets are immutable (232ms)
    √ Stake exposes no authority address (CL-29) (201ms)

  VF-DEP-001 · issuance inactive until finalization
    √ verifyAndMint reverts before the ceremony is closed (113ms)

  CL-01 · VF-ORC-007 signed price records
    √ the arbitrary-issuance parameter no longer exists (193ms)
    √ recordFeeAndRac no longer accepts a caller USD value (197ms)
    √ accepts a batch signed by the price publisher (232ms)
    √ rejects a batch signed by anyone else (219ms)
    √ rejects a tampered price with a valid signature over different data (210ms)
    √ VF-SEC-005: any address may submit; the submitter gains no authority (216ms)
    √ VF-ORC-008: a run must be newer than the last accepted run (220ms)
    √ VF-ORC-005: a zero price marks the asset unavailable rather than substituting one (221ms)
    √ a signature cannot be replayed against a different deployment (248ms)

  CL-01 · price publisher is immutable
    √ VF-DEP-002: a zero price publisher cannot be constructed (209ms)
    √ no function can change the price publisher (201ms)

  CL-39 · run watermark cannot be pushed beyond recovery
    √ rejects a run near the uint64 maximum (206ms)
    √ rejects an advance one beyond the permitted window (201ms)
    √ accepts an advance at the permitted boundary (205ms)
    √ the window advances with the watermark, so long gaps stay recoverable (189ms)
    √ normal consecutive runs are unaffected (222ms)

  CL-37 · stale price records fail closed
    √ a fresh record has a usable valuation (219ms)
    √ remains usable just inside 48 hours (225ms)
    √ is no longer usable past 48 hours (208ms)
    √ an unavailable asset is never usable regardless of age (199ms)
    √ an asset never priced has no usable valuation (198ms)
    √ a new batch restores usability after staleness (258ms)
    √ age is measured from the signed fetch timestamp, not from submission (235ms)

  CL-11 · VF-COM-006 handshake allowance registry
    √ registers a one-use mechanism (111ms)
    √ registers a three-use mechanism (97ms)
    √ VF-COM-006: rejects any allowance other than 1 or 3 (127ms)
    √ an unregistered environment reports zero (104ms)
    √ allowance cannot be registered after finalization (201ms)
    √ allowance cannot be changed by a stranger during the ceremony (118ms)
    √ the package field is no longer consulted anywhere in enforcement

  End-to-end · full verification pipeline via MockChainVerifier
    √ a valid handshake mints VCLM (236ms)
    √ CL-01: issuance is impossible without a published price (191ms)
    √ CL-37: issuance fails closed once the price is stale (218ms)
    √ VF-XCH-011: a package contradicting the source facts is rejected (219ms)
    √ VF-XCH-006: an unfinalized source event is rejected (235ms)

  CL-11 · handshake allowance consumption, end to end
    √ VF-COM-007: a three-use mechanism permits exactly three, rejects the fourth (274ms)
    √ VF-COM-007: a one-use mechanism permits exactly one, rejects the second (237ms)
    √ allowance is per identity, not global (265ms)
    √ CL-11: the caller's asserted allowance count is ignored entirely (246ms)
    √ VF-COM-008: a rejected attempt consumes no allowance (313ms)

  CL-12 · Dev Fund destination enforcement
    √ VF-FEE-006: a substituted destination is rejected (266ms)
    √ the registered destination is accepted (282ms)
    √ an empty destination is rejected even if the registry were empty (254ms)
    √ VF-FEE-008: missing fee transfer evidence is rejected (203ms)
    √ VF-FEE-009: an unconfigured environment cannot issue (157ms)
    √ VF-FEE-009: an empty destination cannot be registered at all (103ms)

  CL-06 · epoch reward basis is populated from RAC credits
    √ the verifier accumulates a running per-epoch reward basis (222ms)
    √ credits accumulate additively within one epoch (244ms)
    √ closeEpoch reads the basis instead of leaving it zero (279ms)
    √ an epoch with no credits has a zero basis and pays nothing (263ms)

  CL-07 · the Stake contract actually mints epoch rewards
    √ a staker is paid through closeEpoch, allocateEpoch and claim (447ms)
    √ VF-STK-004: the reward is newly minted VCLM, raising total supply (305ms)
    √ VF-STK-015: an epoch with no stakers allocates nothing (242ms)

  CL-09 · epoch processing cost must not grow with position count
        closeEpoch gas: 5 positions=109284, 60 positions=109284, ratio=1.00x
    √ closeEpoch gas is bounded as positions accumulate (1098ms)
        closeEpoch gas: 5=109284, 600=109284, ratio=1.000x
    √ closeEpoch gas is flat from 5 to 600 lifetime positions (4307ms)
        allocateEpoch gas: 5=53271, 60=53271, ratio=1.00x
    √ allocateEpoch gas is bounded as positions accumulate (661ms)

  CL-09 · differential oracle — accumulator vs specification
    √ agrees with brute force across 20 randomized positions and 6 epochs (600ms)
    √ agrees after a withdrawal cancels future contribution (205ms)
    √ conservation: registered weight matches the accumulator at every boundary (319ms)

  CL-41 PROOF OF EXPLOIT · unvalidated assetPrecision in recordFeeAndRac
        honest credit: 600000000000000000
        attack credit: 600000000000000000
        inflation:     1x
    √ a caller can inflate the epoch reward basis by understating decimals (166ms)

  CL-41 · pkg.assetPrecision is economically inert
        credits by declared precision [0,6,12,18,255]: 600000000000000000, 600000000000000000, 600000000000000000, 600000000000000000, 600000000000000000
    √ credit is identical across 0, 6, 12, 18 and 255 declared precision (214ms)

  CL-41 · precision divisor comes from the registry
    √ no USD derivation divides by pkg.assetPrecision

  CL-42 · custody class must not silently default
    √ VF-SEC-003: an unrecognized custody class is rejected at registration (115ms)
    √ the three valid classes are accepted (106ms)
    √ custody path is bounded to native or token (98ms)

  CL-43 · asset precision must be within the executable domain
    √ a precision beyond safe arithmetic is rejected at registration (133ms)
    √ precisions across the real range are accepted (135ms)

  CL-44 · output token discriminator is closed
    √ VF-COM-020: every value outside {0,1} is rejected before any minting (280ms)
    √ VCLM (0) mints VCLM and not CHONX (175ms)
    √ rejection precedes emission-rate selection, so no invalid rate is ever used

  CL-45 · staking token discriminator is closed
    √ VF-STK-002: every value outside {0,1,2} is rejected (164ms)
    √ all three valid tokens can be staked (200ms)

  CL-46 · maximum multiplier product does not overflow
    √ SYNTH at 120 days — the largest product — computes exactly 8x (162ms)
    √ a very large stake at maximum multipliers does not revert (152ms)
    √ the weight expression keeps the uint256 operand leftmost

  CL-47 · duration discriminator admits only the specified terms
    √ §10.1: an unlisted duration is rejected (213ms)

  CL-76 · forged package mints against the production UtxoChainVerifier
    1) mints VCLM to an unprivileged caller with no lock on any chain
    2) the production verifier returns finalized=true for an invented block
    3) repeats: a second forged handshake mints again from a distinct phantom lock

  Base Commitment Vault — creation
    √ creates a lock, splits the fee, and isolates principal in its own contract (235ms)
    √ accepts all 16 permitted durations and rejects durations adjacent to them (362ms)

  Base Commitment Vault — negative cases (Standard §5.2)
    √ rejects an unapproved asset WITHOUT moving any of the user's funds (242ms)
    √ rejects a fee-on-transfer asset rather than locking a different amount than recorded (252ms)
    √ rejects a duplicate lock id (205ms)
    √ rejects a zero base recipient and a zero release destination (183ms)
    √ rejects a standard lock below $10.00 (197ms)
    √ rejects a Handshake outside the $0.95-$1.05 band (203ms)
    √ rejects a fourth Handshake from the same identity (240ms)
    √ rejects a CHONX output with no activation receipt (195ms)
    √ rejects ETH sent alongside an ERC-20 lock (182ms)

  Base Commitment Vault — valuation is derived, not asserted
    √ the same gross amount qualifies or fails purely on the oracle price (379ms)
    √ records the derived USD value in the immutable lock record (227ms)

  Base Commitment Vault — release
    √ refuses to release before maturity (226ms)
    √ releases at maturity to the bound destination when called by a stranger (220ms)
    √ refuses a second release (231ms)
    √ releases without consulting the verifier, the price feed, or the factory (259ms)

  Base Commitment Vault — isolation by construction
    √ releasing one lock leaves every other lock untouched (306ms)
    √ the cloned implementation cannot be re-initialized (226ms)
    √ the implementation contract itself is permanently disabled (211ms)

  Base Commitment Vault — configuration
    √ refuses lock creation before configuration is finalized (153ms)
    √ refuses asset registration after finalization, and from a non-deployer (193ms)

  CL-48 · fetchTimestamp is bounded in both temporal directions
    √ BACKWARD: a record older than 48 hours is not usable (CL-37) (138ms)
    √ FORWARD: a future fetchTimestamp is rejected at submission (136ms)
    √ a future timestamp cannot extend the freshness window, because it cannot be stored (112ms)
    √ a fetchTimestamp equal to the current block is accepted (117ms)

  CL-49 · runId cannot wrap, skip beyond recovery, or be reused
    √ the maximum permitted advance succeeds (123ms)
    √ one beyond the permitted advance is rejected (103ms)
    √ a run id cannot be reused, so no batch can be replayed (116ms)
    √ no earlier run id can be accepted after a later one (176ms)

  BaseSameChainVerifier — rejects forged evidence
    √ reverts on a lock that does not exist, however well-formed the proof (228ms)
    √ ignores the caller's claimed amounts and returns vault storage instead (246ms)
    √ ignores the finality proof entirely (259ms)
    √ derives the block hash and height rather than echoing the caller's (256ms)

  BaseSameChainVerifier — accepts genuine locks
    √ verifies a real lock and reports it finalized (245ms)
    √ reports an unknown lock as unverifiable without reverting (210ms)

  BaseSameChainVerifier — construction
    √ refuses a zero vault address
    √ binds the vault immutably at construction (231ms)

  Base end-to-end — genuine lock through the production verifier

    Base e2e: minted 1725.0 VCLM against a real lock

    √ mints against a real Base lock, called by an unprivileged relayer (323ms)
    √ element 9 — the same lock cannot mint twice (322ms)
    √ element 10 — a failed attempt consumes nothing and the lock stays mintable (359ms)
    √ a package naming a lock that was never created cannot mint (317ms)

    Base verifyAndMint gas: 213527

    √ element 8 — records the gas cost of same-chain verification (297ms)

  Sha256dHeaderChain — proof of work
    √ accepts a real header whose work meets its declared target (49ms)
    √ accepts consecutive headers in one call (68ms)
    √ rejects a header whose nonce has been altered (45ms)
    √ rejects a header with an altered merkle root (42ms)
    √ rejects a header whose parent is unknown
    √ rejects a malformed header length (41ms)
    √ rejects a difficulty change off a retarget boundary (39ms)

  Sha256dHeaderChain — chain state
    √ is idempotent: resubmitting a known header is a no-op (41ms)
    √ reports confirmation depth from the best tip (45ms)
    √ reports zero confirmations for an unknown block
    √ accumulates work rather than counting height
    √ refuses a zero checkpoint

  Sha256dHeaderChain — merkle inclusion
    √ verifies the coinbase of a single-transaction block (57ms)
    √ rejects a transaction that is not in the block (65ms)
    √ reverts for an unknown block


  166 passing (42s)
  3 failing

  1) CL-76 · forged package mints against the production UtxoChainVerifier
       mints VCLM to an unprivileged caller with no lock on any chain:
     Error: VM Exception while processing transaction: reverted with custom error 'VerifierNotImplemented("utxo")'
    at UtxoChainVerifier.verifyFinality (contracts/chain-verifiers/UtxoChainVerifier.sol:58)
    at VinculumFinalisVerifier.verifyAndMint (contracts/VinculumFinalisVerifier.sol:792)
    at VinculumFinalisVerifier.verifyAndMint (contracts/VinculumFinalisVerifier.sol:840)
    at EdrProviderWrapper.request (node_modules\hardhat\src\internal\hardhat-network\provider\provider.ts:453:41)
    at HardhatEthersSigner.sendTransaction (node_modules\@nomicfoundation\hardhat-ethers\src\signers.ts:185:18)
    at send (node_modules\ethers\src.ts\contract\contract.ts:313:20)
    at Proxy.verifyAndMint (node_modules\ethers\src.ts\contract\contract.ts:352:16)
    at Context.<anonymous> (test\10_cl76_forged_package.test.cjs:167:5)
  

  2) CL-76 · forged package mints against the production UtxoChainVerifier
       the production verifier returns finalized=true for an invented block:
     Error: VM Exception while processing transaction: reverted with custom error 'VerifierNotImplemented("utxo")'
    at UtxoChainVerifier.verifyFinality (contracts/chain-verifiers/UtxoChainVerifier.sol:58)
    at EdrProviderWrapper.request (node_modules\hardhat\src\internal\hardhat-network\provider\provider.ts:453:41)
    at staticCallResult (node_modules\ethers\src.ts\contract\contract.ts:337:22)
    at staticCall (node_modules\ethers\src.ts\contract\contract.ts:303:24)
    at Proxy.verifyFinality (node_modules\ethers\src.ts\contract\contract.ts:351:41)
    at Context.<anonymous> (test\10_cl76_forged_package.test.cjs:194:25)
  

  3) CL-76 · forged package mints against the production UtxoChainVerifier
       repeats: a second forged handshake mints again from a distinct phantom lock:
     Error: VM Exception while processing transaction: reverted with custom error 'VerifierNotImplemented("utxo")'
    at UtxoChainVerifier.verifyFinality (contracts/chain-verifiers/UtxoChainVerifier.sol:58)
    at VinculumFinalisVerifier.verifyAndMint (contracts/VinculumFinalisVerifier.sol:792)
    at VinculumFinalisVerifier.verifyAndMint (contracts/VinculumFinalisVerifier.sol:840)
    at EdrProviderWrapper.request (node_modules\hardhat\src\internal\hardhat-network\provider\provider.ts:453:41)
    at HardhatEthersSigner.sendTransaction (node_modules\@nomicfoundation\hardhat-ethers\src\signers.ts:185:18)
    at send (node_modules\ethers\src.ts\contract\contract.ts:313:20)
    at Proxy.verifyAndMint (node_modules\ethers\src.ts\contract\contract.ts:352:16)
    at Context.<anonymous> (test\10_cl76_forged_package.test.cjs:211:5)
  



